server:
  port: 8080
  host: localhost

logging:
  level: no-requests
  folder: "./logs"

emitter:
  enabled: false
  path: "/events"

routes:
  - path: /*
    name: allServer
    strategy: round-robin
    health_timeout_ms: 500
    node_request_limit: 100
    docker:
      image: node-server
      internal_port: 3000
      no_requests_timeout_ms: 5000
    pool:
      inactive_size: 2
      active_size: 2
    servers:
    